{"id":1161025096,"question_id":295,"lang":"csharp","lang_name":"C#","time":"2 years, 6 months","timestamp":1706619472,"status":10,"status_display":"Accepted","runtime":"744 ms","url":"/submissions/detail/1161025096/","is_pending":"Not Pending","title":"Find Median from Data Stream","memory":"140.1 MB","code":"public class MedianFinder {\n\n    PriorityQueue<int, int> max = new(Comparer<int>.Create((x, y) => y.CompareTo(x)));\n    PriorityQueue<int, int> min = new(Comparer<int>.Create((x, y) => x.CompareTo(y)));\n   \n    public MedianFinder() {\n    }\n    \n    public void AddNum(int num) {\n\n       \n         // always add to maxheap\n        //max hea[ holds first half part]\n        \n        max.Enqueue(num,num);\n\n        // check size of both heap\n\n        if(max.Count - min.Count > 1)\n        {\n           Console.WriteLine(max.Peek());\n            min.Enqueue(max.Peek(), max.Dequeue());\n           \n        }\n\n        // if first half have greater element\n         if(max.Count > 0 && min.Count > 0 )\n        {\n            if(max.Peek() > min.Peek())\n            {\n                min.Enqueue(max.Peek(),max.Dequeue());\n                max.Enqueue(min.Peek(),min.Dequeue());\n                \n            }\n        }\n    }\n    \n    public double FindMedian() {\n        if(min.Count == max.Count)\n        {\n            return (double)(min.Peek() + max.Peek()) /2 ;\n        }\n        return max.Peek();\n    }\n}\n\n/**\n * Your MedianFinder object will be instantiated and called as such:\n * MedianFinder obj = new MedianFinder();\n * obj.AddNum(num);\n * double param_2 = obj.FindMedian();\n */","compare_result":"111111111111111111111","title_slug":"find-median-from-data-stream","has_notes":false,"flag_type":1,"frontend_id":320}