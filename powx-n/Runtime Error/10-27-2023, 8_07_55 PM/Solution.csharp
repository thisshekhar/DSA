// https://leetcode.com/problems/powx-n

public class Solution {
    public double MyPow(double x, int n) {
        if(n==0)
        {
            return 1;
        }
        bool flag = n >0 ? true : false;
        n = Math.Abs(n);
        if(n == 0)
        {
            return x;
        }

        return flag ? x  * MyPow(x, n-1) : 1/(x  * MyPow(x, n-1));
    }
}